// A styled component exported for use by another module, exercising the
// cross-module (proxy function) code path of #styled.

public #styled ExportedCard("div") {
    color: rebeccapurple;
    border: 1px solid #999;
}
