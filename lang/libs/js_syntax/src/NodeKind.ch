// Single authoritative JavaScript/JSX AST node-kind enum.
//
// `js_parser` and `universal_parser` consume this. It is the union of the two
// former per-package enums; the only divergence was at the tail (`RegexLiteral`
// vs `Paren`), so both are appended here. Do NOT define a second `JsNodeKind`.
public enum JsNodeKind {
    VarDecl,
    FunctionDecl,
    Literal,
    ChemicalValue,
    Identifier,
    MemberAccess,
    FunctionCall,
    ExpressionStatement,
    Block,
    If,
    Return,
    BinaryOp,
    ArrowFunction,
    ArrayLiteral,
    ObjectLiteral,
    For,
    While,
    DoWhile,
    Switch,
    Case,
    Break,
    Continue,
    TryCatch,
    Throw,
    Ternary,
    UnaryOp,
    ForIn,
    ForOf,
    Spread,
    ClassDecl,
    Import,
    Export,
    Yield,
    Debugger,
    ComponentDecl,
    JSXElement,
    JSXExpressionContainer,
    JSXText,
    JSXFragment,
    JSXAttribute,
    JSXSpreadAttribute,
    IndexAccess,
    ArrayDestructuring,
    RegexLiteral,
    Paren
}
