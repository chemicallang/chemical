import "../../../std/vector.ch"

func test_vectors() {
    /** generics not working in different files due to on demand generation
    test("vector of ints work", () => {
        var v = vector<int>();
        v.push(10);
        v.push(20);
        return v.size() == 2 && v.get(0) == 10 && v.get(1) == 20
    })
    test("can remove from vector", () => {
        var v = vector<int>();
        v.push(10);
        v.push(20);
        v.remove(1);
        return v.size() == 1 && v.get(0) == 10
    })
    test("can change elements in vector", () => {
        var v = vector<int>();
        v.push(10);
        v.push(20);
        v.set(0, 30);
        v.set(1, 40);
        return v.size() == 1 && v.get(0) == 30 && v.get(1) == 40
    })
    **/
}